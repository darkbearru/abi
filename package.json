{
  "name": "ai-book-illustrator",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for the AI Book Illustrator platform.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build:packages && npm run build:apps",
    "build:packages": "npm run build -w @abi/shared && npm run build -w @abi/ai-core && npm run build -w @abi/storage && npm run build -w @abi/prompts && npm run build -w @abi/validation && npm run build -w @abi/book-parser",
    "build:apps": "npm run build -w @abi/api && npm run build -w @abi/web && npm run build -w @abi/worker",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run build:packages && npm run typecheck --workspaces --if-present",
    "pretest": "npm run build:packages",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.28.0",
    "eslint": "^9.28.0",
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-vue": "^10.2.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.33.1",
    "vue-eslint-parser": "^10.1.3"
  }
}
